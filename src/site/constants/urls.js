
const domain = 'http://localhost:5000';

export const CRUD_COMPANY = `${domain}/company`;

export const GET_PLUGIN_QUERY = `${domain}/plugin/query`;

export const PROJ_PlUGIN_UPDATE = `${domain}/proj/plugin/update`;

export const GET_PROJ_PLUGINS = `${domain}/proj/plugins`;

export const GET_PLUGINS = `${domain}/plugins`;
